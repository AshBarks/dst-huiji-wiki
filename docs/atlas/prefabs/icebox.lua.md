# `prefabs/icebox.lua`

- 扫描角色：prefabs/icebox.lua
- 归属变体（1 个）：icebox
## 关联

### 组件
- `components/container.lua`：icebox（Direct；line 77）
- `components/inspectable.lua`：icebox（Direct；line 76）
- `components/lootdropper.lua`：icebox（Direct；line 85）
- `components/placer.lua`：icebox（HelperExpanded；line 101）
- `components/workable.lua`：icebox（Direct；line 86）

### 预制体依赖
- `collapse_small`：icebox（Direct；line 100）

### 生成引用
- `collapse_small`：icebox（Direct；line 27）


## 函数

### fn  [46–98]
- 归属：icebox

### onbuilt  [40–44]
- 归属：icebox

### onclose  [19–22]
- 归属：icebox

### onhammered  [24–31]
- 归属：icebox

### onhit  [33–38]
- 归属：icebox

### onopen  [14–17]
- 归属：icebox

