[package]
name = "dst-ktex"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Don't Starve Together KTEX 容器解析与 DXT1/3/5、RGB(A) 解码（ktech 兼容）"
readme = "README.md"
keywords = ["dst", "ktex", "texture", "dxt", "modding"]
categories = ["multimedia::images", "parser-implementations"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE"]

[dependencies]
image = { version = "0.25", default-features = false }
# libsquish 的纯 Rust 移植（MIT）：DXT1/3/5 块解码/压缩
texpresso = "2"
thiserror = "2"
