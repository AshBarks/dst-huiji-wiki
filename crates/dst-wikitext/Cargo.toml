[package]
name = "dst-wikitext"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MediaWiki wikitext 的无损（round-trip）解析与编辑"
readme = "README.md"
keywords = ["mediawiki", "wikitext", "parser", "round-trip"]
categories = ["parser-implementations"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE"]

[dependencies]
